// Actions methods
// GET "/user/login"
const login = (req, res) => {
  res.send('NEVER USED');
};

// GET "/user/logout"
const logout = (req, res) => {
  res.send('NEVER USED');
};

// GET "/user/register"
const register = (req, res) => {
  res.send('NEVER USED');
};

// Controlador user
export default {
  // Action Methods
  login,
  logout,
  register,
};
