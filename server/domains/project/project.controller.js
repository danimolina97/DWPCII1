// Actions methods
// GET "/project"
const showDashboard = (req, res) => {
  res.send('req is defined but never used');
};

// GET "/project/add"
const add = (req, res) => {
  res.send('req is defined but never used');
};

// Controlador user
export default {
  // Action Methods
  showDashboard,
  add,
};
